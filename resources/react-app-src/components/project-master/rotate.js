var React = require('react');

var Rotate = React.createClass({

    render: function(){
            return (
                <div className="rotate">
                    
                </div>
            );
        }
});

module.exports = Rotate;